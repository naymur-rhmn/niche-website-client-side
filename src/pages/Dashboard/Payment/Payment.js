import { Typography } from '@mui/material';
import React from 'react';

const Payment = () => {
    return (
        <Typography variant="h3">Payment is Comming soon</Typography>
    );
};

export default Payment;